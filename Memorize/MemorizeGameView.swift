//
//  ContentView.swift
//  Memorize
//
//  Created by Ubeyt Demir on 5.10.2023.
//

import SwiftUI

// In swift ui pretty much everything is struct!

struct MemorizeGameView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello,iOS!")
        }
        .padding()
     
    }
}
















#Preview {
    MemorizeGameView()
}
